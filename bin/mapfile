#!/bin/bash

# root path project
__root="$(cd "$(dirname "$(dirname "${BASH_SOURCE[0]}")")" && pwd)"

# defaults
file="${__root}/config/mapfile.sql"
output="${__root}/mapfiles/mapfile.csv"

# execute the runner with a preset of parameters
"$(dirname "$0")"/runner "--csv|--file=${file}" "${output}" "$@"

exit $?;
